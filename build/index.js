// document.querySelectorAll("a").forEach((i) => {
//     i.onclick = (e) => {
//         e.preventDefault();
//         // chrome.tabs.create({ url: e.target.getAttribute("href") });
//     };
// });
